To run this project, first install the packages in the requirements text file,
go into the Mesa directory, and type "pip install -r requirements.txt" on the
command line

once all the packages are installed, type "mesa runserver"
